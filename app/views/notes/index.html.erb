<h1>Listing notes</h1>

<table>
  <tr>
    <th>Note</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @affaire.notes.each do |note| %>
  <tr>
    <td><%= note.note %></td>
    <td><%= link_to 'Show',  affaire_note_path(@affaire, note) %></td>
    <td><%= link_to 'Edit', edit_affaire_note_path(@affaire, note) %></td>
    
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Note', new_affaire_note_path(@affaire) %>
